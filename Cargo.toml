[package]
name = "radish"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread", "sync"] }
walkdir = "2"

[dev-dependencies]
tempfile = "3"
